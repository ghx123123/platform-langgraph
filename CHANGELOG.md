# 开发日志 (Changelog)

> 多智能体协作平台 — 各阶段开发记录

---

## 2026-08-12 — 成果中心内容编排与教案数据贯通

- 成果中心新增“内容编排”工作区，可按进度表、教学大纲、知识大纲、教师智能体回答、教师审核稿和思政建议筛选并预览内容，再指定插入到讲次、目标、知识点、重难点、方法、手段、教学过程、评价、课程思政或教学后记。
- 插入支持覆盖、前置和追加三种方式；操作前自动保存教师未提交的编辑，操作后生成课程设计新版本，并记录来源名称、原文定位、目标区域、处理方式、摘要和时间，保证成果可追溯。
- 课程设计创建时冻结所选进度讲次、大纲要求和知识大纲版本；历史设计可从精确版本恢复来源快照，上游资料单元删除后仍保留已确认知识范围，不把恢复内容误标为原始大纲证据。
- 修复课程设计刚创建仍处于排队状态时提前同步空白智能体结果、未完成会话覆盖教案、教师审核稿污染教学后记等严重数据问题；只有同源且已完成的会话可被成果中心引用。
- 多智能体结构化结果新增课程思政元素；教师智能体的教学设计、讲授和答疑可逐项插入指定区域。教学后记保留为教师独立编辑字段，整体同步不会覆盖教师课后反思。
- Word 模板增强进度、目标、授课内容、教学设计、课程思政和教学后记等标签识别；同一合并单元格中的教学过程与思政内容合并写入，不再互相覆盖，并解除目标行固定高度以避免长文本被裁切。
- 模板未预留的非空字段追加到“平台补充内容”，不再静默丢失；内置标准模板补齐知识点章节，完整输出讲次、目标、知识点、重难点、思政、教学过程、评价和教学后记。
- 真实“附件2：理论课程教案格式.docx”验收识别 12 类原位字段，另外 3 类内容进入补充区；教师回答、进度与大纲内容、知识点、思政和教学后记均可检索，相关目标行无固定高度。既有成果原页预览为 5 页 PDF，Chrome `1680×1050` 三栏为 `220 / 1070 / 390px`，无页面级横向溢出或控制台错误。
- 内容插入成功提示改为优先保留，后台来源刷新不再覆盖操作结果；左侧长课程标题不再产生横向滚动。课程设计专项测试 `38 passed`，完整后端回归 `248 passed`；前端 ESLint 与生产构建通过。

---

## 2026-08-12 — 教案原格式导出与成果历史

- 教案导出新增模板预检：识别正文、表格、页眉和页脚中的课程名称、讲次、知识点、目标、重难点、教学过程和评价等字段；不兼容模板会阻止原格式导出，必须由教师明确改用内置标准模板，不再静默降级。
- 模板填充保留 Word 段落样式、run 字体属性、纸张、页边距、表格和页眉页脚；修复“授课时间”误匹配“课时”、说明文字误匹配评价槽位以及跨 run 占位符无法正确处理的问题。
- 资料库中只建立索引的 DOCX 教案模板现可直接搜索和选择，选择后才按需加载原件并预检；同名模板显示父目录，避免大批模板难以辨认。
- 每次导出持久化设计版本、模板来源、匹配字段、SHA-256、文件大小和时间，并复用文档服务提供 Word 下载和 PDF 原页预览；成果历史支持单项删除，删除课程设计或课程资料库时同步清理导出副本。
- 教案定稿页补齐讲次、地点和知识点大纲编辑；左侧设计列表显示真实导出次数，中台成果中心按真实导出记录统计，不再把“已审核但未导出”稿件算作成果。
- 真实资料验收：从 252602 课程资料库按需加载“附件2：理论课程教案格式.docx”，识别 8 类字段和 3 个表格；浏览器导出 DOCX 71,003 字节，纸张、四边距、3 个表格形状和样式集合与模板一致，转换得到 5 页、310,220 字节 PDF 原页预览。Chrome `1680×1050` 三栏无重叠和横向溢出，控制台与网络请求无错误。
- 完整后端回归 `240 passed`；前端 ESLint 零警告，TypeScript/Vite 生产构建通过。

---

## 2026-08-11 — 数据中台目录完整性与渐进加载

- 调研 File Browser、Filestash、Nextcloud Files、TanStack Virtual 和 react-window，采用成熟文件管理器的“先目录摘要、后当前目录内容”模式；调研记录见 `docs/数据中台文件管理优化调研_2026-08-11.md`。
- 修复有章节资料与 393 份未归类资料混合时，“全课程”单元未建立导致文件不可见的问题；真实 1659 文件课程由 1266/1659 恢复为 1659/1659。
- 新增 `summary_only` 和 `unit_id` 目录查询：首屏只返回课程树与统计，当前资料单元按需加载；切换章节不再重新传输整库文件块。
- 前端新增当前资料单元局部加载状态、请求序号防旧响应覆盖，并消除 React Strict Mode 下的重复初始化请求。
- 目录栏新增“已载入 X/Y 份 · 当前目录 N 项”，搜索取消 100 条硬截断，教师可以直接确认资料是否加载完整。
- 数据中台移除自动章节范围导航，改为“一门课程一个目录”；左侧只选择课程，中间主目录按本地来源路径显示全部文件，章节元数据仅供后续资料分析和教学设计使用。
- 本地目录扫描改用可剪枝的安全遍历，预先跳过 `.venv` 等环境目录、符号链接和不可访问项，避免单个异常链接导致整次索引失败；目录卡片数量改为当前文件夹的直接子项数。
- 左侧课程目录补齐“学期 → 课程 → 主目录 → 文件夹”的渐进树，文件夹展开与中间面包屑同步；课程主目录提供快捷新建入口。学期目录支持重命名，并一次更新该学期下的全部课程元数据。
- 目录布局匹配改用文件夹 ID 集合；后端启用 GZip。真实样本首屏从约 2.4 秒/1.15 MB 降到约 125 ms/3.3 KB，最大章节 523,196 B 压缩到 33,254 B。
- Chrome 验证 10 个资料单元、最大章节 583/583、深层目录展开、无横向/整页纵向溢出及控制台错误。

---

## 2026-08-11 — 轻量索引、按需导入与进度反馈

- 数据中台的“导入文件”和“导入文件夹”改为只提交文件名、相对路径、大小和修改时间，不在初始导入阶段上传原件或提取正文；因此初始建索引不再受 80 MB 多文件批次或 360 MB 单文件上传限制。
- 每次文件或文件夹选择都作为课程主目录下的独立一级来源根展示；移除旧“添加来源文件夹”入口，统一为“导入文件 / 导入文件夹 / 关联本地路径”。
- 教师在资料整理页选定本讲次材料并确认后，平台才上传所选浏览器原件或读取所选本地路径文件，再执行正文提取、备课包和课程设计创建；未选文件保持 `metadata_only`，不生成平台副本。
- 数据中台新增可见索引进度条，资料整理页新增所选原件按需导入进度；单文件来源和文件夹来源分别调用文件选择器和文件夹选择器刷新授权。
- 浏览器页面重载后若本地文件权限失效，平台明确提示返回数据中台刷新对应一级来源并重新选择原文件或文件夹，不再误报后端未启动。
- 真实 Chrome 验收：单文件初始索引仅请求来源登记与目录组织，没有 `/files`；重载后重新授权提示正确；刷新单文件来源实际打开文件选择器；选定 1 份资料后才调用 `/files`、`/extract`、`/prepare` 和课程设计接口。`1440×1000` 无横向或整页纵向溢出，索引进度条可见。
- 针对性后端测试 `23 passed`，前端 ESLint 零警告，TypeScript/Vite 生产构建通过。

---

## 2026-08-11 — 多来源文件夹、增量刷新与系统打开

- 一个课程资料库现可登记多个独立来源文件夹；同名文件按来源隔离，来源目录和嵌套子目录会合并到课程主目录中展示。
- 来源分为“浏览器索引”和“本地关联”：浏览器索引在教师选定备课材料后才按需保存所选原件；本地关联保存后端可访问路径，可直接扫描新增、修改和移除的文件及子文件夹。
- 浏览器来源先登记清单；后续教师明确选中的原件按每批最多 100 个文件、约 72 MB 分批上传，避免大文件请求中断后误报后端未启动。
- 文件支持按资料类别或修改日期排序；单击文件使用操作系统默认程序打开，本地关联文件可从更多菜单单独“重新加载”，无需重新扫描整个来源。
- 来源文件夹卡片提供刷新、系统打开和移除图标。移除仅需确认，无需输入文字；只删除平台记录、目录引用和平台副本，绝不删除本机源文件或源文件夹。
- 网络异常提示改为“请求在到达后端前中断”，不再把代理中断、大文件上传中断等情况一律描述为后端未启动。
- 真实接口及 Chrome 验收：同一课程同时关联 2 个本地来源共 34 份文件；新增嵌套文件后刷新正确报告 `added=1`；单文件重新加载替换文档标识并清理旧副本；系统打开文件与目录均返回 `200`；浏览器来源完成首次 3 文件上传、修改 1 项并新增 1 项后的刷新，以及无输入确认移除。两类来源移除后本机测试文件均保持存在。
- 完整后端回归 `181 passed`；前端 ESLint 零警告，TypeScript/Vite 生产构建通过；Chrome `1440×1000` 无横向溢出、控制台错误或旧版“后端未启动”误报。

---

## 2026-08-11 — 课程根目录删除与关联数据清理

- 修复课程资料库根节点没有删除入口的问题：数据中台左侧每个课程根目录新增独立删除图标，删除操作不再触发课程展开或折叠。
- 课程树改为按资料库 ID 建树，不再把同一学期内名称相同的两个资料库合并；课程统计同步按实际根节点计数，根节点显示其原始资料数量。
- 新增 `GET /api/course-archives/{archive_id}/deletion-impact` 删除影响预检，确认框分项显示课程资料、平台文件副本、教学设计、教学资料包和关联会话数量。
- 删除课程前必须输入“删除”；后端统一级联清理课程记录、目录布局、平台文件副本、课程设计、教学资料包及显式关联会话。本机源目录不受影响。
- 使用隔离临时课程完成真实 Chrome 验收：预检显示 1 份资料、1 个副本、1 个设计、1 个资料包和 1 个布局；删除后各资源接口均返回 404，目录与布局归零。两个正式课程及其 3 个资料库记录保持完整。
- 完整后端回归 `179 passed`，前端 ESLint 零警告，TypeScript/Vite 生产构建通过。

---

## 2026-08-11 — 原件优先的数据中台与按需提取

- 重新统一目录关系：课程单元直接作为“主目录”，原始文件、上传文件夹和教师自建文件夹进入同一棵树，不再并列显示旧“原始资料”虚拟根。历史 `system_parent=original` 布局读取时自动迁移；与主目录同名的历史文件夹会合并子目录和文件引用。
- 主目录及子目录文件夹卡片新增可见删除图标，保留重命名、移动等更多操作；新建弹窗实时显示“主目录 / 父目录 / 新文件夹”的创建后位置，成功提示同步给出完整落点。
- 数据中台上传改为原件优先：可在当前目录上传单个文件或完整文件夹，保留文件夹层级，只保存原文件并登记元数据，不在上传阶段提取正文。
- 后端可访问目录的首次扫描与增量更新同样改为 `metadata_only`；原文件持久化后，仅在教师进入备课、明确选择本次材料时调用 `/api/course-archives/{archive_id}/extract` 按需提取。
- 数据中台界面只展示原始文件、原页预览和文件信息；提取正文与生成内容不再作为中台目录项显示，避免资料管理与教学设计混杂。
- 新增原始文件单项删除、批量删除和非空文件夹递归删除；递归删除同步清理子目录、平台托管副本和目录引用，不删除外部索引源文件。
- 修复文件行 `content-visibility` 绘制隔离导致更多菜单被后续列表内容遮挡、删除按钮无法点击的问题；菜单打开时仅解除当前行的绘制隔离。
- 真实接口验证本地目录登记 2 个文件、`parsed_files=0`、2 个原文件标识可用；指定 2 份资料按需提取后仅这 2 份变为 `parsed`，未选资料保持 `metadata_only`。
- 真实 Chrome 逐项验证原页预览、单文件删除、2 文件批量删除、含子文件夹和文件的递归删除；`1280×800`、`1600×1000` 页面尺寸与视口一致，无页面溢出。针对性后端测试 `18 passed`，前端 ESLint 与生产构建通过。

---

## 2026-08-11 — 目录式数据中台与渐进路径选择

- 新增持久化虚拟目录管理：支持系统分类下新建文件夹、自定义子文件夹嵌套、文件与文件夹重命名和移动、文件批量移动，以及空文件夹删除；同级重名、移动到自身/子目录和非空目录删除均会被明确拦截。
- 上传文件夹时可指定“导入后的文件夹名称”，解析完成后在“原始资料”和“提取正文”中分别建立同名独立目录，并自动切换到新建课程单元。目录操作只改变平台布局和显示名称，不改名、移动或删除教师电脑上的源文件，避免破坏下游引用。
- 新增目录布局 API：`POST /api/data-hub/folders`、`PATCH/DELETE /api/data-hub/folders/{folder_id}`、`PATCH /api/data-hub/blocks/{block_id}`、`POST /api/data-hub/blocks/move`、`POST /api/data-hub/archives/{archive_id}/import-folder`；删除课程资料库时同步清理对应目录布局。
- 真实 Chrome 端到端导入 2 份文件，上传返回 `201`，独立目录归档返回 `200`，2 个系统分区共归档 4 个数据块；刷新后目录仍存在，子文件夹创建可用。`1280×800` 与 `1600×1000` 均无横向溢出或控制台错误，验收资料和临时布局已清理。
- 数据中台由一次性文件平铺改为三级渐进工作区：左侧学期/课程/单元树，中部虚拟文件目录与面包屑，右侧文件详情仅在打开文件后出现；单元根目录默认只显示原始资料、提取正文、教学设计、师生讨论、督导建议、思政元素等分类文件夹。
- 支持原始资料实际目录逐级进入、连续重复路径去重、目录名称悬浮查看、当前单元搜索与类型筛选；筛选会回到单元根目录并自动收起，无结果时提供明确恢复动作。
- 新增“选择资料路径”弹窗，区分“上传文件夹”和“索引后端目录”；上传前可按需展开文件清单，后端目录保留原路径并支持增量更新，连接错误直接显示在弹窗内。
- 文件详情支持原页、提取内容和来源三种视图；缺失原页时提供重新连接目录动作。批量选择栏列出已选文件，预览打开时自动让位，切换资料单元会清空上一单元选择并提示，避免跨单元隐性混入。
- 修复导入失败仍显示成功、无提示时目录区高度不足、课程信息可能更新到错误资料库等状态问题；路径弹窗补齐初始焦点、焦点圈定、Escape 关闭和焦点恢复。
- 真实 Chrome 验收目录进入、面包屑、搜索清除、筛选、原页/正文/来源预览、批量选择、资料包目标确认和双模式路径弹窗；`1280×800` 与 `1600×1000` 无横向溢出、遮挡或控制台错误。后端回归 `174 passed`，前端 ESLint 零警告，TypeScript/Vite 生产构建通过。

---

## 2026-08-11 — 平台逻辑审计、上下文防串联与数据链可视化

- 由教师工作流、数据血缘、前端去重和质量验收四个角色并行审计，建立 `docs/平台逻辑重构需求基线_2026-08-11.md`，统一课程资料库、资料单元、备课任务、生成运行、教师定稿、资料包和导出文件术语。
- 成果中心拆分为 `/exports/lesson` 教案定稿和 `/exports/compose` 教学资料包；直接访问、刷新、前进后退均保持子页面，完成会话“编辑与导出”连续 20 次均进入教案定稿。
- 新生成会话持久化 `archive_id/design_id/document_id`；单文件上传、演示和重置会清除旧设计上下文；后端在同步教案前校验课程、设计和主文档，8 类错配测试及在线 API 均返回 409。
- 修复教学资料包初始化竞态：从数据中台进入后必须明确选择“新建”或指定既有资料包；新建、定向追加、取消三条浏览器路径均不再误写最近资料包，慢请求等待 12 秒未覆盖教师选择。
- 统一“成果编排/当前成果”等残留名称为“教学资料包”；当前列表超过 40 项时显示负担提示并二次确认，浏览器实测 188 项误选可取消并停留在数据中台。
- 启动教学设计前新增显式范围确认：系统可推荐知识点，但教师必须确认知识点、课时和深度；任何范围变更都会使确认失效，浏览器验证启动按钮状态为禁用、确认启用、变更禁用、重新确认启用。
- 新增七节点线性数据链：原始文件、正文提取、章节结构、范围确认、智能体生成、教师定稿、导出文件；每个节点显示状态、数量、耗时、问题和下一步动作，详情按需展开，不遮挡三列教学区。
- 真实 Chrome `1600×1000` 验收七节点无溢出，展开高度 105px，提取问题可跳转资料单元；完整后端回归 `170 passed`，前端 ESLint 零警告，TypeScript/Vite 生产构建通过。

---

## 2026-08-11 — 桌面交互梳理、原页预览与可编辑 Word 导出

- 按 `ui-ux-pro-max`、Vercel Web Interface Guidelines 和 Apple Design 的渐进披露、低动效、高密度工作台规范完成批判性审计，确认主要问题为卡片堆叠、选择状态弱、预览入口过深、来源库过载和导出入口重复。
- 数据中台由两列资料卡片改为紧凑文件表格；课程树、资料筛选和文件操作形成稳定层级，搜索文件时不再清空左侧课程树。
- 新增持续可见的“已选资料”条，列出类型和文件名，支持逐项移除、清空及批量加入成果编排；批量跳转和异步追加经真实浏览器验证。
- 数据中台与资料整理页新增页面内原页预览抽屉，支持 PDF 及转换后的 DOCX/PPTX；历史记录缺少预览地址时明确提示重新连接原资料目录。
- 修复预览接口被 `X-Frame-Options: DENY` 拦截的问题，调整为 `SAMEORIGIN` 并增加 `Content-Security-Policy: frame-ancestors 'self'`，只允许本站工作区内嵌。
- 资料整理页新增备课包文件清单，逐项展示已选文件并标出主材料，支持清空和单项移除；每行同时提供页面内预览和新窗口打开原文件。
- 成果编排改为“选择内容、编辑整理、预览导出”三步流程；统一内容库按教学设计、问答、思政元素、原始资料分组，支持当前单元/全部课程切换、批量勾选、来源预览和批量插入。
- 去除重复 DOCX 导出按钮，将主操作明确命名为“导出可编辑 Word (.docx)”；前端下载前校验 Word MIME 和 ZIP `PK` 签名，Markdown/JSON 收入次级交换格式。
- 真实 Chrome `1600×1000` 验收：DOCX 原页转为 22 页 PDF、另一份教材预览为 101 页；浏览器下载 Word 为 91,658 字节，`python-docx` 成功读取 2,034 个段落。完整回归 `161 passed`，ESLint 零警告，TypeScript/Vite 构建通过。

---

## 2026-08-11 — 跨课程数据中台与成果编排

- 顶层工作区扩展为“数据中台、资料单元、课程设计、成果编排”四页，支持多个学期和多个课程资料库的统一视图。
- 新增全局内容块契约，将原始文件、提取正文、教学设计、学生问题、教师答疑、督导建议、思政元素和导入内容统一为可检索、可引用的数据资产。
- 对未显式关联课程设计的历史会话，按课程名称与章节自动归档；现有正式数据索引出 9 个章节单元、1,659 份原始资料和 1,306 个内容块。
- 新增本地目录扫描接口，保存本地根路径和最后扫描时间；增量扫描复用未变化解析结果。真实接口验证首次新增 2 份、二次更新 1 份且 1 份保持不变。
- 新增成果编排持久化：可组合教学设计、学生问题和思政元素，编辑内容与顺序，保存版本；保存后的派生内容回流中台，可被其他成果再次引用。
- 支持 DOCX/PDF/PPTX/Markdown/TXT/中台 JSON 导入，提供网页编辑、页面内预览和独立打印预览；支持 DOCX、Markdown、中台 JSON 三种导出。
- 真实浏览器验收创建“第3章传感器教学资料包”，混合 3 类内容，三种格式下载成功；导出 DOCX 为 37,644 字节且关键字段可由 `python-docx` 读取。
- 自动化回归 `161 passed`；前端 ESLint 零警告、TypeScript 与 Vite 生产构建通过；`1600×1000` 数据中台与成果编排页无横向溢出和控制台/API 错误。

---

## 2026-08-11 — 三阶段课程设计与模板教案导出

- 将教师主流程拆分为 `/materials` 资料整理、`/design` 课程设计和 `/exports` 文件输出三个可独立跳转的桌面页面，避免在单页堆叠全部任务。
- 新增持久化课程设计模块和 8 组 API，保存原始文件、提取正文、结构化进度讲次和多智能体生成成果四层引用；引用可回到原文件、原页预览或完整提取正文。
- 资料页选择章节、进度表讲次、多份配套材料和主材料后，同时组装备课包并建立课程设计记录；课程设计仍按同一知识范围多轮打磨。
- 文件输出页支持课程元数据、目标、重难点、方法、手段、教学过程、评价、课程思政和后记编辑，保存版本并同步已完成的多智能体会话。
- 支持资料库 DOCX 模板、临时上传 DOCX 模板和内置标准模板；关键词槽位支持右侧单元格、下方单元格和 `{{field}}` 占位符，导出文件保持可编辑。
- 模板材料 ID 与上传文档 ID 均写入课程设计及版本快照，重新进入页面可恢复选择；启动会话成功但设计关联失败时保留会话并给出可恢复操作，不再误报整次启动失败。
- 真实浏览器验收：第 3 章设计记录保留 29 条上游引用，使用“附件2：理论课程教案格式.docx”成功导出 69,838 字节 Word；课程名称、主题、目标、重难点、教学方法/手段和教学过程均写入模板。
- 自动化回归：后端正式测试 `157 passed`，前端 ESLint 零警告，TypeScript 与 Vite 生产构建通过；`1600×1000` 三阶段页面无横向溢出或浏览器控制台/API 错误。

---

## 2026-08-11 — 学期资料库与证据驱动备课包

- 新增学期资料库：完整遍历课程文件夹，按 13 类教学材料建立清单，识别章节、讲次、版本、SHA-256 重复组与 Excel 教学进度。
- 新增桌面三栏资料工作区：章节与类别筛选、资料矩阵、主材料指定、讲次绑定、历史备课习惯、证据展开、经验复制和备课包创建。
- 从真实学期资料中归纳课程约束对齐、同范围多轮打磨、工程案例组织、对比与系统链、OBE 映射、课时约束、多格式成果、质量审核和 Word 归档等可复用习惯。
- 资料库建库采用快速索引：PDF 使用 PyMuPDF 原生文本，DOCX 使用 `python-docx` 段落与表格；扫描件和 Office 图片的完整 OCR 继续由单文档上传链路负责，避免对整学期资料执行高成本 OCR。
- 清理 Word OCR 常见页眉、点引导线、页码、页脚和分隔式中文标题；真实第 3 章提取为 42,158 字、64 个内容分区，并恢复传感器定义、组成、性能指标、电感式传感器、码盘等知识点。
- 备课包上下文限制为 7,800 字，为视觉证据预留 2,000 字，修复总上下文超过 10,000 字导致的 422；前端校验错误显示具体失败字段。
- 真实文件夹验收：1,659 份资料、9 个章节、46 份已读正文、319 个重复组；首次建库约 80–93 秒，Chapter 3 的 14 份材料组装备课包约 0.4–0.5 秒。
- 浏览器验收：章节切换、资料多选、讲次绑定、经验复制、DOCX 原页转 PDF、材料预览、三列多智能体结果和同范围“再跑一轮”均通过；DOCX 预览响应为 5.56 MB PDF。
- 自动化验证：正式测试 `149 passed`，前端 TypeScript 与 Vite 生产构建通过。遗留 `backend/tests/test_report_api.py` 仍有 5 个与旧版报告接口断言相关的失败，未纳入当前正式测试目录。

---

## 2026-08-10 — Qwen-MM-Plugins 启发的逐页视觉复核

- 保留 `PyMuPDF + RapidOCR v6` 全文解析主链，新增 PDF/DOCX/PPTX 指定页视觉增强，不用整套 MCP 替换业务架构。
- 页面按视觉 patch 网格动态缩放，提供快速、标准、精细三档预算和不外发的 dry-run。
- 新增逐页视觉分析与历史读取 API，结果包含摘要、图表/公式/版式要素、备课提示、OCR 修正、置信度、耗时和警告。
- OCR 修正必须能在本地提取文本中定位；空结构自动重试，失败不覆盖历史有效结果。
- 原页预览增加视觉复核入口、实时秒数、结构化证据、复制和折叠；结果自动作为补充上下文进入教学设计。
- 真实 Python 教材第 16 页视觉调用与桌面/移动端浏览器验收通过；正式后端测试 142 项通过，前端生产构建通过。

## 2026-08-10 — PDF 开源解析器实测与 RapidOCR v6 升级

- 用 39 页扫描教材、8 页原生报告和 20 页混合材料实测 `pdf-inspector`、`book-to-skill`、CnOCR 与 RapidOCR v6。
- 确认 `pdf-inspector` 适合原生 PDF 分类/结构提取但不提供扫描 OCR；`book-to-skill` 不适合作为底层识别引擎。
- 扫描页及 Word 嵌入图片改为 RapidOCR v6 首选、CnOCR 自动回退，解析报告显示实际引擎和回退原因。
- 指定教材 39/39 页识别，同方式直接解析耗时由约 37.41 秒降至 25.10 秒，核心主题覆盖保持一致，封面人工抽查准确性明显改善；真实 HTTP 暖机上传为 45.66 秒，已作为后续性能项记录。
- 扫描封面章节号与标题分行时自动合并课程名，避免工作区只显示“第10章”。
- 针对 Python 教材这类“整页扫描图 + 错误隐藏 OCR 层”的 PDF，新增逐页检测、原图 OCR、逐页置信度记录和分区页码映射；真实结果由约 19,687 字符提升到 22,931 字符，结构分区为 25 个，20/20 页覆盖。
- 前端目录点击分区可定位 PDF 原页，实测 `2.3.6` 分区跳转到第 16 页。
- 新增 OCR 坐标排序、低置信度过滤和回退测试；完整记录见 `docs/PDF开源识别工具实测_2026-08-10.md`。

---

## 2026-08-01 — 今日更新总览（UX 完善 + 课本实测 + 内容深化 + 逐功能验证）

> 全天工作分 4 个阶段推进，完整明细见 **docs/今日更新记录_2026-08-01.md**。

| 阶段 | 成果 | 状态 |
|------|------|------|
| ① UX 完善（多智能体团队） | 首屏引导 + 示例课程、知识点编辑器、会话卡片/进度条 | ✅ |
| ② 真实课本实测迭代 | 解析/流程/前端/内容 4 环节，修复 P1-P8 | ✅ |
| ③ 内容深化 | 课堂练习、教师/学生版导出、预计剩余时长、代码复制 | ✅ |
| ④ 浏览器逐功能验证 | 7 组 30+ 检查点通过；修复 WS 不重连 Bug（P13） | ✅ |

**关键数据**：后端 103 passed；前端 tsc + vite build 通过；13 项问题修复（P1-P13）；55 个文件变更。

---
## 2026-08-01 — 浏览器逐功能验证 + WebSocket 重连修复

### 背景

启动真实前后端服务，用 Chrome 自动化逐个功能验证前端，覆盖 7 组 30+ 检查点。

### 验证结果（全部通过）

| 组 | 功能 | 结果 |
|---|------|------|
| A | 页面加载 / 顶栏 / 欢迎面板 / 会话列表 | ✅ |
| B | 上传课本 PDF / 标题识别与修改 / 补充要求 | ✅ |
| C | 知识点编辑器（重点切换/改标题/增删） | ✅ |
| D | 模型设置（切换/测试连接/保存） | ✅ |
| E | 断点暂停与放行 / 消息流 / 四个右侧面板 / 导出按钮 | ✅ |
| F | 会话卡片信息 / 继续迭代 / 复制全文 / 两步删除 / 停止 / 移动端 | ✅ |

### 发现并修复：WebSocket 不重连导致 UI 卡死

- **问题**：已完成会话的 WS 推送 stream.closed 后关闭；再次"继续迭代"让会话回到运行态时，
  useWorkflowEvents 只依赖 runId，不会重连 → 完成后 UI 一直停在"教学进行中"，需手动刷新。
- **修复**：useWorkflowEvents 增加 status 依赖，终态→运行态时自动重连 WebSocket；
  App.tsx 传入 selectedRun?.status 并调整声明顺序。
- **验证**：继续迭代后卡片在 1 秒内自动更新为"第 N/N 轮 · 已完成"。

### 验证环境

- Chrome 自动化 + 真实课本 PDF；测试后现场已恢复：测试会话删除、模型还原 MiniMax-M3（密钥保留）、服务停止。
- 截图留存：frontend/screenshots/verify-*.png。

---
## 2026-08-01 — 内容深化：课堂练习 + 教师/学生版导出 + 运行体验

### 背景

承接真实课本实测迭代，落地内容设计分析文档中的高价值建议，补齐"练习-评估-交付"闭环。

### 新增：结构化课堂练习

- teaching_design 产出 exercises（level: low/medium/high + question + answer），覆盖基础/进阶/拓展三层。
- 模型未返回时按知识点自动兜底生成 3 道分层练习；mock 模式同样生成。
- 前端"教学环节"面板展示练习卡（层级徽章 + 可展开答案）；报告（MD/PDF）新增"课堂练习"章节。

### 新增：教师版 / 学生版教案导出

- 报告接口新增 ?variant=teacher|student（非法取值 422）。
- 教师版（默认）：完整教案 + 练习答案 + 督导评价 + 课堂记录。
- 学生版：学案式——学习目标 + 教学环节 + 课堂练习（隐藏答案），不含督导评价与课堂记录；
  前端"教学成果"面板提供"教师版 PDF / 学生版 PDF / 导出 Markdown"三个按钮。

### 新增：运行体验

- TeachingProgress 新增"预计还需 X分Y秒"胶囊：按已完成阶段/总阶段比例外推，运行中每秒刷新。
- Markdown 代码块新增一键复制按钮（讲授稿中的 Python 示例可直达 IDE）。

### 验证

- 后端 103 passed（新增 4 个报告测试：练习章节、学生版隐藏答案、学生版 PDF、文件名后缀）。
- 前端 tsc 与 vite build 通过；Chrome 端到端：3 层练习卡渲染与答案展开、三个导出按钮、
  教师版/学生版 MD 与 PDF 差异正确；SSR 验证代码复制按钮与预计剩余时长渲染。
- 测试后现场已恢复：删除测试会话、模型配置还原 MiniMax-M3（密钥保留）、服务停止、临时文件清理。

---
## 2026-08-01 — 真实课本实测迭代（《Python 程序设计基础与应用》第 2 章）

### 背景

使用真实课本 PDF（book/python程序设计基础与应用(OCR)课本2.pdf，OCR 扫描件）按环节实测：
解析 → 剖析 → 设计 → 讲授 → 分层提问 → 答疑 → 督导 → 成果，每环节记录问题、修复后进入下一环节。

### 环节 1：文档解析（修复 4 项）

- 课程名识别"第X章"式教材标题（原退回文件名）。
- 重写知识点抽取：编号小节标题（2.2/2.2.3）识别、代码/注释/OCR 噪声行过滤、辅助标题排除。
- 新增 fix_ocr_text 纠错表（Pyhon/逆辑运算符/二雄码/字符申 等），下游 LLM 不再被噪声污染。
- 实测：12 个知识点全部有意义；新增 tests/test_document_parser.py（5 例）。

### 环节 2：教学全流程（修复 4 项）

- 各节点把"候选知识点"作为权威锚点传入模型（content_analysis/teaching_design/teach_knowledge/teacher_answer）。
- mock 内容生成优先取候选知识点，剥离《》等符号噪声。
- 轮次重点跳过章级标题（如"内置对象、运算符、表达式、关键字"），聚焦具体知识点；对照知识点保证与重点不同。
- 真实模型（MiniMax-M3）1 轮实测 141s：产出质量优秀（代码示例、认知冲突、三层答疑、专业督导点评）。

### 环节 3：前端交互

- 真实 9.3MB PDF 上传→解析→知识点编辑→启动→完成全链路通过。
- 启动区新增耗时预期提示（本地演示约 10 秒 / 真实模型 1 轮约 2-3 分钟，可随时停止）。

### 环节 4：内容设计

- 讲授稿 prompt 强化课堂可执行性：禁用网页标签/Markdown 表格，要求 2 个"停顿口答→公布答案"课堂小练习（修复督导实测指出的 <details> 不可触发问题）。
- 新增《docs/内容设计分析.md》：基于实测证据的 7 条教师/学生双视角内容设计原则。
- 新增《docs/课本实测迭代记录.md》：全环节问题清单与修复对照。

### 验证

- 后端 99 passed（94 原有 + 5 解析器新增）；前端 tsc 与 vite build 通过。
- 回归：上传课本 → mock 2 轮完成（评分 86、6 条分层提问、轮次聚焦正确）→ 耗时提示与进度胶囊正常。
- 测试后现场已恢复：删除 6 条测试会话、模型配置还原 MiniMax-M3（密钥保留）、服务停止、临时文件清理。

---
## 2026-08-01 — 用户视角体验完善（多智能体团队协作）

### 背景

以新用户视角走查平台，发现 4 个体验断点：首次进入缺少引导与快速体验入口、
上传后无法确认/编辑 AI 抽取的知识点、历史会话看不出评分与轮次、运行过程缺少
"第几轮 / 跑了多久"的进度感。本次由 3 个 worker 智能体并行实现 + 主线集成。

### 新增：首屏引导与一键示例

| 能力 | 实现 |
|------|------|
| 引导面板 | 中栏空状态升级为 WelcomePanel：价值主张 + 参与角色（教师/分层学生/督导）+ 三步使用说明 + 双操作按钮 |
| 一键体验 | frontend/src/lib/demoCourse.ts 内置《光合作用与细胞呼吸》示例课程，点「先看看效果」即填入表单 |
| 材料就绪态 | 已载入课程但未启动时，中栏提示调整知识点后点击启动 |

### 新增：启动前知识点编辑

- KnowledgePointEditor：上传后即可对候选知识点「设/取消重点、内联改标题、删除、新增」，
  改动实时同步到启动请求（knowledge_points 由用户确认后的副本提交）。

### 新增：会话列表与运行进度

- SessionRunCard：会话卡片显示状态点、时间、状态；已完成会话带「综合评分 N 分」徽章与「第 N/M 轮」；
  paused 紫色「待你处理」；failed 悬停展示错误；两步删除确认在卡片内部完成。
- TeachingProgress：中栏标题区常驻「第 N/M 轮」+「已运行 X分Y秒」胶囊，每秒刷新，终态定格，暂停态变色。

### 验证

- 后端 94 passed；前端 tsc 与 vite build 通过。
- Chrome 端到端冒烟：欢迎面板渲染 → 一键载入示例（9 个知识点/3 重点）→ 编辑器增删改标星 →
  启动 mock 全流程 2 轮完成（14 条消息、综合评分 86、PDF/Markdown 导出正常）。

---
## 2026-07-26 (二) — 成果闭环 + 聊天式界面重构

### 背景

浏览器实跑演示发现：后端生成的 1325 字教学成果报告（`final_output`）**前端从未渲染**，
全页 27 个按钮无一可导出。用户跑完 7 分钟拿不到任何可交付物，产品闭环断裂。

### 新增：教学成果与导出

| 能力 | 实现 |
|------|------|
| 教学成果面板 | 右侧新增第 4 个标签页，渲染 `final_output` |
| 导出 Markdown | `GET /api/workflows/runs/{id}/report.md` |
| 导出 PDF | `GET /api/workflows/runs/{id}/report.pdf`（11 页 A4，中文字体自动注册） |
| 复制全文 | 前端剪贴板，失败时静默降级 |
| 顶部快捷导出 | 运行完成后标题栏出现「导出成果」 |

`backend/workflows/report.py` 为新写实现。**未复用** `teaching/pdf_report_service.py`——
后者 1264 行且深度耦合旧版 `TeachingSession`/`QuizResult` 模型，改造量大于重写。
PDF 生成为 CPU 密集同步调用，用 `asyncio.to_thread` 避免阻塞事件循环。
中文文件名按 RFC 5987 编码并保留 ASCII 回退。

### 性能：学生提问并发化

三类学生互不依赖，由串行改为 `asyncio.gather` 并发（`gather` 保序，仍为 拓展→进阶→基础）。

| 阶段 | 优化前 | 优化后 |
|------|--------|--------|
| student_question | 约 80 秒（3×25s 串行） | **24.8 秒** |

### 界面：聊天式重构

| 问题 | 改动 |
|------|------|
| 讲授稿 Markdown 未渲染，`##`/`**`/表格全是裸文本 | 引入 `react-markdown` + `remark-gfm`，新增 `.md` 排版样式（标题/列表/表格/引用/代码） |
| 角色无视觉区分 | 教师靠左绿色、学生靠右橙色、督导通栏红色；位置 + 色彩双重编码 |
| 轮次不可辨 | 新增粘性吸顶的轮次分隔条（「教学准备」「第 N 轮教学」） |
| 长文淹没对话流 | 超过 420 字默认折叠，标题栏显示「展开全文 · N 字」，底部渐隐 |
| 运行中只有空 loading | 新增「正在工作」指示器，把当前节点翻译成"谁在做什么" |
| 标签页写死 3 列 | 改为 4 列，新增「教学成果」 |

### 测试

新增 `tests/test_report_export.py` 4 个用例：Markdown 分节完整性、PDF 合法性与非空、
文件名非法字符清洗、**并发后学生提问顺序稳定性**。

**83 → 87 passed**；前端 `tsc && vite build` 通过。

---

## 2026-07-26 — 教学产出质量优化 + 仓库整理

### 背景

以产品经理和用户视角实跑平台并逐项测试，发现演示模型产出与上传文档无关等问题。

### P0：演示模型产出与上传文档无关（已修复）

**问题**：`workflows/llm.py` 的 `_mock_response` 按 system prompt 关键字返回硬编码文案。
上传《光合作用》与《嵌入式系统》，内容剖析、讲授、提问、答疑**完全相同**；
多轮迭代的讲授稿也逐轮雷同，"教学迭代"名存实亡。

**修复**：新增 `_prompt_line` / `_prompt_block` / `_salient_terms`，从 prompt 中还原课程标题、
材料正文与本轮重点，据此生成学科专属内容。标题行优先，含谓语的叙述句降级，避免把整句当知识点。

| 验证项 | 结果 |
|--------|------|
| 两学科重点交集 | 空集 |
| 跨学科讲授稿相同 | 否 |
| 同课程两轮讲授相同 | 否（第 2 轮换重点并响应上轮督导） |

### P1：其他质量问题（已修复）

| 问题 | 位置 | 修复 |
|------|------|------|
| 课程标题取文件名而非文档标题 | `documents/service.py` | 新增 `_course_name()`，优先取正文 H1，回退文件名 |
| 文档主标题被当成知识点 | `documents/service.py` | 抽取时排除与课程名相同的条目 |
| 知识点带"第一章"编号前缀 | `documents/service.py` | 剥离章节编号，保留知识点本名 |
| 重点判定写死前 3 条 | `documents/service.py` | 改为按条目数的 1/3 配额，显式标注优先 |
| 难度分级仅匹配"难"字 | `documents/service.py` | 扩展至"难/综合/推导" |
| `review_threshold=85` 从不生效 | `workflows/graph.py` | 接入 `route_iteration`，达标提前结束并发 `iteration.stopped` 事件 |
| 学生提问引用讲授稿开场白 | `workflows/graph.py` + `llm.py` | 显式传入`本轮重点`/`对照知识点`，不再从散文反推 |
| 知识点抽取有两套实现 | `workflows/graph.py` | `_fallback_points` 改为复用 `extract_knowledge_points()` |

### P0-2：真实模型（推理模型）适配（已修复）

接入 MiniMax-M3 实测后发现两个仅在真实模型下暴露的问题：

| 问题 | 现象 | 修复 |
|------|------|------|
| 推理模型思维链混入 JSON 提取 | `<think>` 块内的花括号被正则 `\{[\s\S]*\}` 捞成 JSON 边界，导致 `teaching_design` 解析失败，教学环节**静默退回通用模板** | `generate()` 统一剥离 `<think>`（含未闭合截断场景）；`generate_json()` 改用括号配平 `_balanced_object()` 提取最外层完整对象 |
| 降级完全静默 | 三处 `except (ValueError, TypeError)` 直接吞异常，日志无记录，用户看不出"AI 没干活" | 补 `logger.warning` + 新增 `node.degraded` 事件，前端可提示本节使用了通用模板 |

**修复前后对比**（MiniMax-M3，同一份《光合作用与细胞呼吸》材料）：

| 项目 | 修复前 | 修复后 |
|------|--------|--------|
| 学习目标 | "准确解释核心概念"（通用兜底） | "模拟卡尔文循环中 CO₂ 固定与 C3 还原过程，建立 C3 与 C5 数量动态模型" |
| 教学环节 | 4 个通用环节（与 mock 相同） | 8 个具体环节（磁性贴片建模、Venn 图比较、出门票评估） |
| 思维链泄漏 | 存在风险 | 无 |

**真实模型耗时参考**：2 轮完整流程约 419 秒（14 次 LLM 调用），1 轮约 122 秒。

### P2：仓库整理

- 归档 26 个根目录一次性调试脚本 → `archive/debug-scripts-202604/`
- 归档 6 个重复/一次性验证脚本 → `archive/oneoff-scripts/`（`ports.bat` 为 `stop.bat` 依赖，已保留）
- 归档 5 份历史报告 → `archive/reports-202604/`
- `SPEC.md` / `ARCHITECTURE.md` 补充实现状态说明：标注 7 个未挂载子系统（21 个文件），
  并补充当前实际运行的架构图

### 测试

新增 4 个端到端测试（`tests/test_langgraph_workflow.py`），覆盖文档驱动产出、逐轮差异、
提问引用真实知识点、评分达标早停。新增 5 个模型解析测试（`tests/test_model_client_parsing.py`），
覆盖思维链剥离、未闭合思维链、含花括号散文中的 JSON 提取、代码块围栏、无 JSON 时报错。

**74 → 83 passed**。真实模型路径已用 MiniMax-M3 端到端验证。

---

## 2026-05-07 — 可视化前端 v1.0

### 新增：多智能体流程可视化系统

**背景**：用户无法看到完整的数据导入、多智能体执行、结果产出流程，缺少可观测性。

### 核心变更

#### 前端新增文件

| 类别 | 文件 | 说明 |
|------|------|------|
| **类型** | `src/types/visualization.ts` | AgentNodeData、ExecutionTimelineEvent、TaskExecution 等 14 个类型 |
| **状态** | `src/stores/importStore.ts` | 数据导入状态（5 阶段解析流程） |
| **状态** | `src/stores/executionStore.ts` | 多 Agent 执行状态（节点/边/任务/消息流/时间线） |
| **工具** | `src/utils/cn.ts` | Tailwind 类名合并（clsx + tailwind-merge） |
| **组件** | `src/components/visualization/` | 可视化组件库（DataImport / AgentGraph / TaskBoard / ResultDashboard） |
| **增强** | `src/components/enhanced/EnhancedDebateWindow.tsx` | 辩论界面增强：图谱+时间线+统计三视图 |

#### 可视化组件清单

**数据导入** (`DataImport/`)
- `UploadProgress.tsx` — 上传进度条 + 四阶段（上传→提取→分析→构建）可视化

**Agent 协作图谱** (`AgentGraph/`)
- `AgentGraphView.tsx` — React Flow 主视图（缩放/拖拽/小地图）
- `AgentNode.tsx` — Agent 节点（6 种状态颜色 + 动画脉冲）
- `MessageEdge.tsx` — 消息流边（计数徽章 + 虚线动画）

**任务看板** (`TaskBoard/`)
- `ExecutionTimeline.tsx` — 执行时间线（8 种事件图标）
- `TaskCard.tsx` — 任务卡片（4 优先级 + 4 状态）

**结果仪表板** (`ResultDashboard/`)
- `StatisticsPanel.tsx` — 统计面板（耗时/Token/消息/响应时间/完成率/Agent 活跃度）
- `KnowledgeRadarChart.tsx` — Recharts 雷达图（知识点覆盖 + 掌握度）

#### 后端新增

| 文件 | 说明 |
|------|------|
| `backend/execution/router.py` | 执行状态 API（8 个端点） |

**API 端点**：
- `GET /api/execution/status` — 执行状态概览
- `GET /api/execution/sessions/{id}/graph` — Agent 协作图谱数据
- `GET /api/execution/sessions/{id}/timeline` — 执行时间线
- `GET /api/execution/sessions/{id}/statistics` — 执行统计
- `POST /api/execution/sessions` — 创建执行会话
- `POST /api/execution/sessions/{id}/agents` — 添加 Agent 到会话
- `POST /api/execution/sessions/{id}/edges` — 添加消息边
- `PUT /api/execution/sessions/{id}/agents/{agent_id}/status` — 更新 Agent 状态
- `POST /api/execution/tasks` — 创建任务
- `PUT /api/execution/tasks/{id}/status` — 更新任务状态

#### 修改文件

| 文件 | 变更 |
|------|------|
| `frontend/src/App.tsx` | DebateWindow 路由切换到 EnhancedDebateWindow |
| `backend/main.py` | 注册 execution router |

#### 新增依赖

```json
"@xyflow/react": "^12.10.2",  // Agent 协作图谱
"recharts": "^3.8.1"          // 统计图表
```

### 验证

- [x] TypeScript 类型检查通过
- [x] Vite 生产构建成功（2400 modules，2.61s）
- [x] 后端 router 注册无错误

---

## 2026-04-23 — 项目初始化

- 创建 SPEC.md（需求规格）和 ARCHITECTURE.md（架构设计）
- FastAPI 后端 + React 前端骨架
- Agent CRUD / Memory / Message API
- WebSocket 实时通信
- MiniMax LLM 集成
- SQLite 持久化
- 辩论子系统（debate/）
- 教学子系统（teaching/）

---

## 2026-04-13 — 协作协议设计

- 标准化消息格式（7 种消息类型）
- Agent 状态机（IDLE → PROCESSING → WAITING/ESCALATE）
- 协作质量评估（5 维度）
- 消息优先级体系（P0-P3）

---

*最后更新：2026-08-11*

---

## 2026-08-30 — 资料单元 ↔ 课程设计 数据衔接（落地）

- **导入前解析检查**：`POST /api/material-units/{id}/import-precheck` 报告每份材料的 `parse_status`，未完整提取的材料被标 `needs_parse`；前端据此触发后台解析（`/parse-tasks` 并发 2），进度条逐份展示，解析完成并持久化后再真正创建课程设计，杜绝进入课程设计后才"二次解析"。
- **教学补充/教学大纲落进设计**：`CourseDesignKnowledgeNode` 扩字段 `description / teacher_note / evidence`，`_resolve_knowledge_outline` 整节点保留进 `source_snapshot.knowledge_nodes`；`assembly_sources` 新增 `outline:{id}:note` 来源，把资料单元生成的 AI 教学补充作为可插入块（默认 target=postscript）进入内容编排。
- **大纲版本快照 + 升级徽标**：课程设计钉住 `material_unit_id + knowledge_outline_id + knowledge_outline_version`，不再每轮漂移；`GET /course-designs/{id}` 返回 `outline_has_newer_version / outline_latest_version`，`POST /course-designs/{id}/rebind` 显式升级到最新版（version+1，保留 `_versions` 历史），解除旧版 409 锁。
- **材料预览默认进「提取文本」模式**：从资料单元导入的课程设计进 `DocumentPreviewWorkspace` 时默认 `defaultView='text'`（不再渲染原始 PDF），教师直接看到 OCR/解析后的正文。
- **导入体验优化**：删除底部"导入课程设计"按钮，顶部统一入口；默认不选全部材料，自动选「知识范围」里已勾选教材节点反查出的材料（无选中时回退到第一本教材），并指定主教材。

## 2026-08-30 — 资料单元 UI 专项优化

- **大纲版本历史列表管理**：右侧大纲面板 header 加「🕘 版本历史」按钮 → 弹窗列出所有版本（v1/v2/v3，状态/节点数/时间），可查看/单个删除/删除全部旧版（被课程设计引用受保护，409）。复用后端 `delete_outline_version` 的引用保护。
- **图谱导入统一管理（图谱=源头）**：教学补充 = 大纲节点自由文本；图谱节点 = 教材研读独立知识单元；导入 = 图谱内容复制进 `teacher_note` + `evidence` 加 `graph:{id}`；取消导入 = 删块 + 删 evidence。后端 `GET /graph-nodes/{node_id}/outline-imports` 反查所有导入位置。
  - 修取消导入的"逆序删"bug：原 `find(startsWith('graph:'))` 只取第一个 → 改为遍历所有 graph evidence，每个图谱一个独立按钮。
  - 修 unlink 的"匹配不到"bug：原按 outline_id 取最新版本（图谱节点可能只在 v4）→ 改为 `outline_id + node_id` 精确匹配含该节点版本。
- **教师提示词生成教学补充（对话框式）**：节点「📖 教学补充」→ 对话框 → 输入提示词（如"补充 C 语言相关知识点"）→ dsh 读该节点+整个大纲结构生成四段（##主题/##要点/##教学建议/##引用原文）→ 预览可编辑 → 同意导入就地保存。后端 `POST /knowledge-outlines/{oid}/nodes/{nid}/teacher-note` 生成候选不回写 + `PUT` 就地更新当前版本 `teacher_note`（不新建版本）。教学补充不再被图谱插入污染。
- **资料单元三列可拖动调宽**：分隔条用 flex 子元素不占列；CSS 变量驱动列宽，方向：拖分隔条向右 → 右侧变窄（分隔条跟随鼠标）；side 200-450px，outline 320-640px，main min 480px。`grid+绝对定位[data-split=0]` 跑屏幕外 (-4) 的坑已改用 flex 修掉。

## 2026-08-30 — dsh agent 化（B 方案：多轮记忆 + 自主迭代）

- **桥协议 v0.3.2**：`scripts/dsh_agent_bridge.py` 新增 `agent_run` 方法——同一 `session_id` 做 N 轮自主迭代，第 1 轮完整执行，第 2..N 轮把"上一轮 final_response + round_focus"作为新 prompt。复用 session 让 dsh 多轮记忆（桥内串起，原 dsh_engine 每次换 `uuid.uuid4()` 是浪费）。`iterations` 桥端 clamp 到 1-5。
- **`ModelClient.agent_iterate` 接口暴露**：dsh provider 走 `engine.agent_run`，非 dsh（mock/openai_compatible）退化为单次 `generate`。`generate_json` 仍保留。
- **`content_analysis` 接入**：单次 `generate_json` 改为 dsh 下的 2 轮 `agent_iterate`，round_focus 强约束"末轮必须是完整合法 JSON 且不要代码块包裹"。末轮用平台 `_extract_json_text` 纯函数提取（不二次调模型），提取失败时回退 `generate_json` 重试兜底。
- **真实教材 A/B 对比**（python 第1章，11132 字 + 大纲 v5）：2 轮迭代产出 11 个成体系知识点（含【概述】/【应用】/【版本演进】式前缀），单次 9 个散点。迭代核心价值是**结构化主线 + 自查补全**，而非数量堆叠。
- **修复 `_extract_json_text` 偶发失败**：minimax 末轮输出偶尔带说明+代码块，`_balanced_object` 第一个 `{` 配平对象 `json.loads` 失败即抛 → 兜底 `analysis={}` → 下游空 dict 让 run 失败。修复：提取失败自动 `generate_json` 重试，日志打末轮原始输出前 300 字便于诊断。

## 2026-08-30 — "看不到生成过程" 三 bug 串联修复

- **Bug 1：uvicorn Win+reload 强 Selector loop → dsh 桥 `NotImplementedError`**
  - 根因：`uvicorn/loops/asyncio.py` 的 `asyncio_setup(use_subprocess=True)` 在 win+reload 时强制 `WindowsSelectorEventLoopPolicy`，而 `asyncio.create_subprocess_exec` 只在 Proactor loop 上支持 → dsh 桥 spawn 失败 → run 卡在 content_analysis、error 字段为空。
  - 铁证：reload 后端 0/3 跑通；非 reload 后端 2/2 跑通到 finalize。
  - **根治**：`backend/workflows/dsh_engine.py` 给桥子进程配**常驻独立 Proactor loop（后台线程）**，与主 app loop 解耦。`generate/agent_run/ensure_started/close` 全部委托 `_run_on_loop`（用 `run_coroutine_threadsafe + wrap_future` 避免跨 loop future 错误）。任何启动方式（reload/非 reload）都能跑通。
- **Bug 2：App.tsx 第 490 行 useEffect 误清 `selectedRunId`（闪一下消失）**
  - 根因：`if (activeDesign && !activeDesign.run_id && selectedRunId) setSelectedRunId(null)`——所有 design 的 run_id 默认为 None，用户主动点选历史 run 立即被清理。
  - 修复：新增 `userSelectedRunRef`，`selectRun` 时置位，清理 effect 尊重"用户主动选择"。
- **Bug 3：`content_analysis` JSON 提取偶发失败**：见上一节。
- **前端"生成过程日志"面板**：`messages=0` 的等待空态改为实时过程日志——从 events 解析 `node.started / node.completed / run.heartbeat`，生成"后端在做什么"的时间线（最多 12 条，节点标签 + 文案 + 时间戳 + 状态色），不再显示"正在建立第一份分析结果"一句话让用户误以为卡死。

## 2026-08-30 — 课程设计迁移到 minimax-m3 已知要点

- 切到 `minimax-m3` 后端默认模型后，`content_analysis` 在 minimax 的 JSON 输出稳定性比 deepseek-v4-flash 略低（末轮偶发说明+代码块包裹），已用 round_focus 强约束+ `generate_json` 重试兜底。`minimax-m3` token plan 限流时（429）由桥的 `translate_error` 返回"模型额度不足（429 限流）"，前端展示为明确提示。
- `dsh_engine.agent_iterate(iterations=2)` 在 minimax 上比 deepseek 慢约 2-3 倍（约 60-90 秒一轮），生成过程视图的 heartbeat 让等待可观察。

## 2026-08-30 — 测试与系统验证

- 后端全量回归 `232 passed`，4 个 PDF/OCR 环境相关测试 deselected（需真实引擎）。新增 6 个 dsh agent 化相关测试（教学补充携带 / rebind 升级 / 版本检测×2 / rebind API / precheck）。
- 真实数据端到端验证：用 python 资料单元的 3 份已解析材料 + 大纲 v5 跑通到 finalize（30 事件，含 11 次 heartbeat、7 个节点 start、6 个节点 complete）。
- 前端 tsc ✓、vite build ✓（仅预存的 `.mu-split` CSS 警告）。

---

*最后更新：2026-08-30*

