# FastAPI and server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=11.0.0
sse-starlette>=1.8.0

# Pydantic
pydantic>=2.0.0
pydantic-settings>=2.2.0

# LangGraph orchestration and OpenAI-compatible model providers
langgraph>=1.0.0,<2.0.0
langchain-openai>=1.0.0,<2.0.0
langgraph-checkpoint-sqlite>=3.0.0,<4.0.0
python-multipart>=0.0.9
pypdf>=4.0.0
PyMuPDF>=1.24.0
# Preferred local OCR for scanned teaching materials (PP-OCRv6 ONNX models).
rapidocr>=3.9.1,<4.0.0
onnxruntime>=1.20.0,<2.0.0
# Optional compatibility fallback when RapidOCR cannot load.
cnocr>=2.3.2
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0
xlrd>=1.2.0
# Open-source Office reading-order/table extraction used before image OCR.
markitdown>=0.1.5
# Windows fallback for converting DOCX/PPTX originals to preview PDFs.
pywin32>=311; platform_system == "Windows"

# Redis
redis>=5.0.0

# Database
aiosqlite>=0.19.0
sqlalchemy>=2.0.0

# Vector search (optional - fallback to SQLite FTS if unavailable)
# qdrant-client>=1.7.0

# MCP SDK
mcp>=1.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=1.0.0
pytest-cov>=4.1.0
httpx>=0.25.0
hypothesis>=6.0.0

# PDF Generation
reportlab>=4.0.0
matplotlib>=3.7.0

# Utilities
python-dotenv>=1.0.0
